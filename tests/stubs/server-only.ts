// Stub for the "server-only" import guard when running unit tests under
// Vitest (plain Node), where Next.js's special bundler resolution for this
// package isn't available. See vitest.config.ts's resolve.alias.
export {}
